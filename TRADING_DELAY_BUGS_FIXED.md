# 거래 지연 버그 수정 문서

이 문서는 재현 가능하고 예측 가능한 거래 지연 버그들의 수정 내역을 설명합니다.

## 수정된 버그 목록

### 1. Grid Engine: 중복된 pending LIMIT 체크 제거

**문제점:**
- `syncGridSimple` 함수에서 `this.pendings["LIMIT"]` 체크가 두 번 연속으로 수행됨 (910줄, 924줄)
- 첫 번째 체크에서 이미 거래가 차단되었는데도 불구하고 두 번째 체크까지 도달하여 불필요한 로그가 발생하고 코드 가독성이 떨어짐

**영향:**
- 코드 중복으로 인한 유지보수 어려움
- 동일한 조건을 두 번 체크하는 비효율성

**수정 내용:**
- 중복된 두 번째 체크 제거
- 단일 체크로 통합하여 로직 단순화

**파일:** `src/strategy/grid-engine.ts` (907-927줄)

---

### 2. Order Coordinator: 주문 생성 성공 후 즉시 잠금 해제

**문제점:**
- `placeOrder`, `placeMarketOrder`, `placeStopLossOrder`, `placeTrailingStopOrder`, `marketClose` 함수들이 주문 생성 성공 후에도 잠금을 유지
- 잠금은 기본 3초 타임아웃 또는 `synchronizeLocks`가 주문 상태를 확인할 때까지 유지됨
- 빠르게 체결되는 주문의 경우, 주문이 이미 체결되었어도 잠금이 남아있어 다음 주문이 최대 3초까지 지연될 수 있음

**영향:**
- 빠른 시장에서 주문 체결 후에도 최대 3초간 다음 주문이 차단됨
- 특히 고빈도 거래 전략에서 심각한 지연 발생

**수정 내용:**
- 모든 주문 생성 함수에서 주문 생성 성공 직후 `unlockOperating` 호출 추가
- 주문 ID는 `pendings`에 저장하여 `synchronizeLocks`가 주문 완료 시 정리할 수 있도록 유지
- 잠금은 주문 생성 직후 해제되지만, `pendings`는 주문이 완료될 때까지 유지되어 중복 주문 방지

**파일:** `src/core/order-coordinator.ts`
- `placeOrder` (169-171줄)
- `placeMarketOrder` (214-215줄)
- `placeStopLossOrder` (298-299줄)
- `placeTrailingStopOrder` (354-355줄)
- `marketClose` (398-399줄)

---

### 3. Maker/Offset-Maker/Trend: 주문 상태 동기화 로직 일관성 개선

**문제점:**
- 각 전략 엔진마다 주문 상태 확인 로직이 다름:
  - **Maker/Offset-Maker**: `status !== "NEW" && status !== "PARTIALLY_FILLED"`일 때 잠금 해제
  - **Trend**: `status !== "NEW"`일 때 잠금 해제
  - **Grid**: `FINAL` 상태 세트 (`FILLED`, `CANCELED`, `CANCELLED`, `REJECTED`, `EXPIRED`)를 명시적으로 체크
- 일관성 없는 로직으로 인해 일부 전략에서는 주문이 완료되지 않았는데도 잠금이 해제되거나, 반대로 완료되었는데도 잠금이 유지될 수 있음

**영향:**
- 전략별로 다른 동작으로 인한 예측 불가능한 지연
- 일부 경우 주문이 아직 진행 중인데도 잠금이 해제되어 중복 주문 발생 가능성

**수정 내용:**
- 모든 전략 엔진을 Grid Engine과 동일한 로직으로 통일
- `FINAL` 상태 세트를 명시적으로 체크하여 주문이 완전히 종료된 경우에만 잠금 해제
- 주문이 open orders 목록에서 사라진 경우 (체결 또는 취소)에도 잠금 해제

**파일:**
- `src/strategy/maker-engine.ts` (256-273줄)
- `src/strategy/offset-maker-engine.ts` (232-249줄)
- `src/strategy/trend-engine.ts` (274-291줄)

---

### 4. Grid Engine: pendingKeyUntil TTL 최적화 및 조기 해제 로직 추가

**문제점:**
- `pendingKeyUntil`의 TTL이 10초로 설정되어 있어, 빠른 시장에서 주문 재시도가 불필요하게 지연됨
- 만료된 항목이 자동으로 정리되지 않아 메모리 누수 가능성
- 주문이 이미 활성화되었거나 체결/취소되었는데도 suppression이 유지될 수 있음

**영향:**
- 빠른 시장에서 주문 재시도가 최대 10초까지 지연됨
- 메모리 사용량 증가 가능성

**수정 내용:**
- TTL을 10초에서 5초로 단축하여 빠른 시장에서 더 빠른 재시도 가능
- 매 틱마다 만료된 `pendingKeyUntil` 항목을 자동으로 정리하는 로직 추가
- 활성 주문이 확인되면 즉시 suppression 제거 (기존 로직 유지)

**파일:** `src/strategy/grid-engine.ts`
- TTL 상수 (104줄): `10_000` → `5_000`
- 만료 항목 정리 로직 추가 (651-657줄)

---

## 수정 효과

### 지연 시간 개선
1. **주문 생성 후 잠금 해제**: 최대 3초 → 즉시 (0초)
2. **pendingKeyUntil TTL**: 최대 10초 → 최대 5초
3. **중복 체크 제거**: 불필요한 조건 체크 제거로 CPU 사용량 감소

### 예측 가능성 개선
- 모든 전략에서 일관된 주문 상태 처리
- 명시적인 FINAL 상태 체크로 예측 가능한 동작

### 안정성 개선
- 메모리 누수 방지 (만료된 suppression 항목 자동 정리)
- 중복 주문 방지 로직 강화

## 테스트 권장 사항

1. **고빈도 거래 시나리오**: 빠르게 연속 주문을 생성하여 지연이 개선되었는지 확인
2. **주문 체결 후 즉시 재주문**: 주문이 빠르게 체결된 후 다음 주문이 즉시 생성되는지 확인
3. **메모리 사용량**: 장시간 실행 시 `pendingKeyUntil` 맵의 크기가 과도하게 증가하지 않는지 확인

## 관련 이슈

이 수정들은 다음 문제들을 해결합니다:
- 빠른 시장에서의 거래 지연
- 주문 생성 후 불필요한 대기 시간
- 전략별 일관성 없는 동작
- 메모리 누수 가능성
